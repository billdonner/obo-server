[project]
name = "obo-server"
version = "0.1.0"
description = "REST API for obo flashcard decks"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "asyncpg>=0.30",
    "pydantic>=2.0",
]

[project.scripts]
obo-server = "obo_server:run"
